import { Bell, CalendarDays, ChartNoAxesCombined, HeartPulse, LayoutDashboard, Menu, Pill, Settings, Trophy, Users, X } from 'lucide-react';
import { useState } from 'react';
import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { useApp } from '../../contexts/AppContext';
const nav=[['/dashboard','Dashboard',LayoutDashboard],['/profiles','Family Profiles',Users],['/medicines','Medicines',Pill],['/schedule','Schedule',CalendarDays],['/adherence','Adherence',ChartNoAxesCombined],['/streaks','Achievements',Trophy],['/period','Period Tracking',HeartPulse],['/notifications','Notifications',Bell],['/settings','Settings',Settings]] as const;
export default function AppLayout(){const [open,setOpen]=useState(false);const {profiles,selectedProfileId,selectProfile,user,logout,notifications}=useApp();const navigate=useNavigate();const unread=notifications.filter(n=>!n.isRead).length;const Sidebar=()=> <><div className="border-b px-5 py-5"><div className="text-xl font-black text-brand-700">Wellora</div><p className="text-xs text-slate-500">Family health companion</p></div><nav className="space-y-1 p-3">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium ${isActive?'bg-brand-50 text-brand-700':'text-slate-600 hover:bg-slate-50'}`}><Icon size={18}/>{label}</NavLink>)}</nav></>;
return <div className="min-h-screen"><aside className="fixed inset-y-0 left-0 hidden w-64 border-r bg-white lg:block"><Sidebar/></aside>{open&&<div className="fixed inset-0 z-50 lg:hidden"><button className="absolute inset-0 bg-black/30" onClick={()=>setOpen(false)} aria-label="Close menu"/><aside className="relative h-full w-72 bg-white shadow-xl"><button className="absolute right-3 top-3" onClick={()=>setOpen(false)}><X/></button><Sidebar/></aside></div>}<div className="lg:pl-64"><header className="sticky top-0 z-30 flex h-16 items-center gap-3 border-b bg-white/95 px-4 backdrop-blur sm:px-6"><button className="lg:hidden" onClick={()=>setOpen(true)}><Menu/></button><select value={selectedProfileId} onChange={e=>selectProfile(e.target.value)} className="input max-w-56">{profiles.map(p=><option key={p.id} value={p.id}>{p.fullName} · {p.relationship}</option>)}</select><div className="ml-auto flex items-center gap-3"><button className="relative rounded-xl p-2 hover:bg-slate-100" onClick={()=>navigate('/notifications')}><Bell size={20}/>{unread>0&&<span className="absolute right-0 top-0 grid h-5 min-w-5 place-items-center rounded-full bg-red-500 px-1 text-[10px] text-white">{unread}</span>}</button><div className="hidden text-right sm:block"><p className="text-sm font-semibold">{user?.fullName}</p><button onClick={()=>{logout();navigate('/login')}} className="text-xs text-slate-500 hover:text-red-600">Sign out</button></div></div></header><main className="p-4 sm:p-6 lg:p-8"><Outlet/></main><footer className="px-6 pb-8 text-center text-xs text-slate-400">Wellora organises reminders and records. It does not provide diagnosis, treatment, or emergency advice.</footer></div></div>}
