import { Award, Flame, Trophy } from 'lucide-react';import { Badge, PageHeader, StatCard } from '../../components/common/UI';import { streaks, useApp } from '../../contexts/AppContext';
export default function StreakPage(){const {selectedProfileId}=useApp();const s=streaks.find(x=>x.profileId===selectedProfileId)!;const milestones=[3,7,14,30,60,100];const next=milestones.find(x=>x>s.current)??100;return <><PageHeader title="Achievements" description="Encouragement for completing required medicine routines."/><div className="grid gap-4 sm:grid-cols-3"><StatCard label="Current streak" value={`${s.current} days`} icon={<Flame/>}/><StatCard label="Longest streak" value={`${s.longest} days`} icon={<Trophy/>}/><StatCard label="Successful days" value={s.totalSuccessfulDays} icon={<Award/>}/></div><section className="card mt-6"><h2 className="font-bold">Next milestone</h2><p className="mt-2 text-sm text-slate-500">Great work! You are {Math.max(0,next-s.current)} day(s) away from the {next}-day achievement.</p><div className="mt-4 h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-brand-600" style={{width:`${Math.min(100,(s.current/next)*100)}%`}}/></div></section><section className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{milestones.map(m=><div className="card" key={m}><Award className={s.current>=m?'text-amber-500':'text-slate-300'}/><h3 className="mt-4 font-bold">{m}-day badge</h3><div className="mt-2"><Badge tone={s.current>=m?'green':m===next?'blue':'slate'}>{s.current>=m?'Achieved':m===next?'In progress':'Locked'}</Badge></div></div>)}</section></>}
