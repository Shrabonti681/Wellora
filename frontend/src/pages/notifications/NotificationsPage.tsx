import { Bell } from 'lucide-react';import { Badge, PageHeader } from '../../components/common/UI';import { useApp } from '../../contexts/AppContext';
export default function NotificationsPage(){const {notifications,markRead,markAllRead}=useApp();return <><PageHeader title="Notifications" description="Medicine, stock, streak, and period updates." action={<button className="btn-secondary" onClick={markAllRead}>Mark all read</button>}/><div className="space-y-3">{notifications.map(n=><button key={n.id} onClick={()=>markRead(n.id)} className={`card flex w-full gap-4 text-left ${!n.isRead?'border-brand-200 bg-brand-50/30':''}`}><div className="rounded-xl bg-white p-2 text-brand-700"><Bell size={18}/></div><div className="flex-1"><div className="flex items-center justify-between gap-3"><h2 className="font-semibold">{n.title}</h2>{!n.isRead&&<Badge tone="blue">Unread</Badge>}</div><p className="mt-1 text-sm text-slate-600">{n.message}</p><p className="mt-2 text-xs text-slate-400">{n.createdAt}</p></div></button>)}</div></>}
